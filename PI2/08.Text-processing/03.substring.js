function main(str, startIndex, count) { 

  // let output  = str.substr(startIndex, count)
  // console.log(output);

  console.log(str.substr(startIndex, count));

}

main("ASentance", 1, 8);